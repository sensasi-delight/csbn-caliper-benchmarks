test:
  name: basic-contract-benchmark
  description: test benchmark
  workers:
    number: 2
  rounds:
    - label: createAsset fixed-load
      description: createAsset benchmark
      txDuration: 30
      rateControl:
        type: fixed-load
        opts:
          transactionLoad: 5
          startingTps: 100
      workload:
        module: workload/createAsset.js

    - label: deleteAsset fixed-load
      description: deleteAsset benchmark
      txDuration: 30
      rateControl:
        type: fixed-load
        opts:
          transactionLoad: 5
          startingTps: 100
      workload:
        module: workload/deleteAsset.js

    - label: reach fixed-load
      description: reach smart contract benchmark
      txDuration: 30
      rateControl:
        type: fixed-load
        opts:
          transactionLoad: 5
          startingTps: 100
      workload:
        module: workload/reach.js

    - label: readAsset fixed-load
      description: Read asset benchmark
      txDuration: 30
      rateControl:
        type: fixed-load
        opts:
          transactionLoad: 5
          startingTps: 100
      workload:
        module: workload/readAsset.js

    - label: readAssets fixed-load
      description: readAssets benchmark
      txDuration: 30
      rateControl:
        type: fixed-load
        opts:
          transactionLoad: 5
          startingTps: 100
      workload:
        module: workload/readAssets.js

    - label: updateAsset fixed-load
      description: updateAsset benchmark
      txDuration: 30
      rateControl:
        type: fixed-load
        opts:
          transactionLoad: 5
          startingTps: 100
      workload:
        module: workload/updateAsset.js
