test:
  name: basic-contract-benchmark
  description: test benchmark
  workers:
    number: 2
  rounds:
    - label: createAsset maximum-rate
      description: createAsset smart contract benchmark
      txDuration: 30
      rateControl:
        type: maximum-rate
        opts:
          tps: 100
          step: 5
          sampleInterval: 20
          includeFailed: true
      workload:
        module: workload/createAsset.js

    - label: deleteAsset maximum-rate
      description: deleteAsset smart contract benchmark
      txDuration: 30
      rateControl:
        type: maximum-rate
        opts:
          tps: 100
          step: 5
          sampleInterval: 20
          includeFailed: true
      workload:
        module: workload/deleteAsset.js

    - label: reach maximum-rate
      description: reach smart contract benchmark
      txDuration: 30
      rateControl:
        type: maximum-rate
        opts:
          tps: 100
          step: 5
          sampleInterval: 20
          includeFailed: true
      workload:
        module: workload/reach.js

    - label: readAsset maximum-rate
      description: readAsset smart contract benchmark
      txDuration: 30
      rateControl:
        type: maximum-rate
        opts:
          tps: 100
          step: 5
          sampleInterval: 20
          includeFailed: true
      workload:
        module: workload/readAsset.js

    - label: readAssets maximum-rate
      description: readAssets smart contract benchmark
      txDuration: 30
      rateControl:
        type: maximum-rate
        opts:
          tps: 100
          step: 5
          sampleInterval: 20
          includeFailed: true
      workload:
        module: workload/readAssets.js

    - label: updateAsset maximum-rate
      description: updateAsset smart contract benchmark
      txDuration: 30
      rateControl:
        type: maximum-rate
        opts:
          tps: 100
          step: 5
          sampleInterval: 20
          includeFailed: true
      workload:
        module: workload/updateAsset.js
